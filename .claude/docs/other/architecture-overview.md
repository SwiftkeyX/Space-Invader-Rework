> AUTO-GENERATED by /read-architecture — do not edit manually. Re-run to refresh.
> Last generated: 2026-06-14

---

## GameManager
**SRP**: Single source of truth for run state: lives remaining, current level index, and whether a run is active, won, or lost.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip | C# event | Low | Subscribes to `OnPlayerDeath` → decrements lives |
| SceneLoader | Direct method call | High | Calls scene-load requests (`LoadGameplay`, `ReloadGameplay`) |
| LevelManager | C# event | Low | Fires `OnLevelChanged`; subscribes to `OnLevelCleared` |
| UIManager | C# event | Low | Fires `OnLivesChanged`, `OnLevelChanged`, `OnRunStarted`, `OnRunEnded`; subscribes to `OnRestartRequested` |
| AudioManager | C# event | Low | Fires `OnRunStarted`/`OnRunEnded` |

---

## SceneLoader
**SRP**: Single gateway for all scene transitions.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Direct method call | High | Receives load requests from GameManager |
| UIManager | C# event | Low | Fires `OnSceneLoaded` so UIManager can bind its UIDocument |
| Any GameLogic/HUD system | C# event | Low | Fires `OnSceneLoaded` for sibling-scene init |

---

## InputManager
**SRP**: Single read point for all player input.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip | Property read + C# event | Low | Exposes `MoveAxis`/`FireHeld`; fires `OnFirePressed` |
| GameManager / UIManager | Property read | Low | `InputEnabled` toggled for menus/game-over |

---

## PlayerShip (`PlayerShipContext` + `PlayerShipStat` + BT nodes)
**SRP**: Player-controlled ship: movement, firing, health/damage, and invulnerability — split across a thin BT coordinator (`PlayerShipContext`), a stat+IDamageable component (`PlayerShipStat`), and orthogonal BT leaf nodes (`MoveAction`, `FireAction`, `InvulnOverlayAction`).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| InputManager | Property read | Low | Reads `MoveAxis`/`FireHeld` via `GameManager.Instance.Input` |
| Projectile | Direct method call | High | Spawns player bullets from pool; `TakeDamage()` called on collision |
| GameManager | C# event | Low | Fires `OnPlayerDeath` → GameManager decrements lives |
| PowerUpSystem | C# event | Low | Subscribes to `OnPowerUpChosen` → applies stat modifiers |
| AudioManager / JuiceManager | C# event | Low | Fires `OnPlayerHit`/`OnPlayerDeath` |

---

## Projectile
**SRP**: The bullet — both player shots and enemy fire — moves, checks team, damages opposing targets, and returns to the pool on hit or lifetime expiry.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip (`PlayerShipStat`) | Direct method call | High | Calls `TakeDamage()` on enemy bullet collision |
| EnemyFireController | Direct method call | High | Enemy bullets spawned and initialized by EnemyFireController |
| Enemy | Direct method call | High | Calls `TakeDamage(damage)` on player bullet collision |
| Object Pool | Direct method call | High | Acquire on spawn, release on despawn |

---

## EnemyFormation
**SRP**: Owns the marching invader grid: spawns the wave, moves the formation in lockstep, reverses at screen edges, and tracks remaining invader count.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted`; fires `OnFormationCleared` |
| Enemy | C# event + Inspector reference | Medium | Spawns/owns Enemy children; subscribes to `Enemy.OnEnemyKilled` |
| EnemyFireController | Property read | Low | Exposes live shooter candidates |
| GameManager | C# event | Low | Formation-reaches-player-line → run-fail signal |

---

## Enemy
**SRP**: A single invader: holds hit points (tiered), takes damage from player projectiles, awards points, and fires `OnEnemyKilled` on death.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Projectile | Direct method call | High | `TakeDamage(amount)` called on player bullet collision |
| EnemyFormation | C# event + Inspector reference | Medium | Owned by formation; fires `OnEnemyKilled` |
| ScoreSystem | C# event | Low | Subscribes to `OnEnemyKilled` → awards points |
| AudioManager / JuiceManager | C# event | Low | Subscribe to `OnEnemyKilled` → SFX, particles |
| EnemyFireController | Property read | Low | Reads position/muzzle when selected to fire |

---

## EnemyFireController
**SRP**: Picks which living invaders shoot, decides firing pattern and cadence, and spawns enemy projectiles — scaling difficulty per level.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted` for per-level fire config |
| EnemyFormation | Property read | Low | Reads live shooter candidate set + muzzle positions |
| Projectile | Direct method call | High | Spawns enemy bullets from pool |
| PlayerShip | Property read | Low | Reads player X position for aimed patterns |

---

## LevelManager
**SRP**: Drives the run's 6 fixed levels: starts each wave, waits for it to clear, triggers the power-up offer, and advances to the next level.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | C# event | Low | Subscribes to `OnLevelChanged`; fires `OnLevelCleared` |
| EnemyFormation | C# event | Low | Fires `OnLevelStarted`; subscribes to `OnFormationCleared` |
| EnemyFireController | C# event | Low | Fires `OnLevelStarted` with fire config |
| PowerUpSystem | C# event | Low | Fires `OnLevelCleared` → power-up offer |
| UIManager / AudioManager / JuiceManager | C# event | Low | Fires `OnLevelStarted`/`OnLevelCleared` |

---

## ScoreSystem
**SRP**: Tracks the run's score: subscribes to kill events, accumulates points, and notifies the HUD.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` → adds point value |
| UIManager | C# event | Low | Fires `OnScoreChanged` → score display |
| GameManager | C# event | Low | Subscribes to `OnRunStarted` → resets score |

---

## PowerUpSystem
**SRP**: The run's roguelike layer — builds a power-up offer after each level and applies the chosen modifier to the player ship.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelCleared` → builds offer |
| UIManager | C# event | Low | Fires `OnPowerUpOffered`; receives selection back |
| PlayerShip | C# event | Low | Fires `OnPowerUpChosen` → stat/weapon modifier |
| GameManager | C# event + Direct method call | High | Subscribes to `OnRunStarted` (Low); `ExtraLife` calls `AddLife` directly (High) |

---

## UIManager
**SRP**: Presentation layer: renders HUD (score, lives, level), between-level power-up pick screen, and game-over/victory/restart UI in UI Toolkit.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | C# event | Low | Subscribes to run/lives/level events; fires `OnRestartRequested` |
| ScoreSystem | C# event | Low | Subscribes to `OnScoreChanged` |
| PowerUpSystem | C# event | Low | Subscribes to `OnPowerUpOffered`; relays selection |
| SceneLoader | C# event | Low | Keys off `OnSceneLoaded` to bind UIDocument |

---

## AudioManager
**SRP**: Plays all music and sound effects in response to game events.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` → kill SFX |
| PlayerShip | C# event | Low | Subscribes to `OnPlayerHit`/`OnPlayerDeath` via `playerShip.Stat` |
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted`/`OnLevelCleared` → level cues |
| GameManager | C# event | Low | Subscribes to `OnRunStarted`/`OnRunEnded` → music |
| ScoreSystem / PowerUpSystem | C# event | Low | Optional score/combo + power-up SFX |

---

## JuiceManager
**SRP**: Owns non-audio game feel: screen shake, particle bursts, and hit-stop (brief `timeScale` freezes).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` → particles + shake |
| PlayerShip | C# event | Low | Subscribes to `OnPlayerHit`/`OnPlayerDeath` → shake + hit-stop |
| LevelManager | C# event | Low | Subscribes to `OnLevelCleared` → celebratory burst |
| Camera | Inspector reference | Medium | Direct ref for shake offset |
| Particle pools | Direct method call | High | Acquire/release pooled burst systems |
| (Whole game) | Direct method call | High | `timeScale` changes for hit-stop |
