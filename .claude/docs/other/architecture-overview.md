> AUTO-GENERATED by /read-architecture — do not edit manually. Re-run to refresh.
> Last generated: 2026-06-13

---

## GameManager
**SRP**: GameManager is the single source of truth for run state: how many lives remain, which of the 6 levels is current, and whether a run is active, won, or lost.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip | C# event | Low | Subscribes to `OnPlayerDeath` |
| SceneLoader | Direct method call | High | Calls intent methods (`LoadGameplay`, `ReloadGameplay`) |
| LevelManager | C# event | Low | Subscribes to `OnLevelCleared`; fires `OnLevelChanged` |
| UIManager | C# event | Low | Fires `OnLivesChanged`, `OnLevelChanged`, `OnRunStarted`, `OnRunEnded`; subscribes to `OnRestartRequested` |
| AudioManager | C# event | Low | Fires `OnRunStarted`/`OnRunEnded` |

---

## SceneLoader
**SRP**: SceneLoader is the single gateway for all scene transitions.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | Direct method call | High | GameManager calls intent methods directly |
| UIManager | C# event | Low | Fires `OnSceneLoaded` for HUD bind timing |

---

## InputManager
**SRP**: InputManager is the single read point for player input.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip | Property read + C# event | Low | Exposes `MoveAxis`/`FireHeld` (read); fires `OnFirePressed` (event) |
| GameManager / UIManager | Direct method call | High | GameManager/UIManager may toggle `InputEnabled` flag (TBD) |

---

## PlayerShip
**SRP**: PlayerShip is the player's avatar: a ship locked to horizontal movement along the bottom strip that fires upward, survives via 3 lives with brief invulnerability frames after each hit, and accepts run-scoped weapon/stat modifications from power-ups.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| InputManager | Property read + C# event | Low | Reads `MoveAxis`/`FireHeld`; subscribes to `OnFirePressed` |
| Projectile | Direct method call | High | Spawns bullets from pool; receives `TakeHit()` from enemy projectiles |
| GameManager | C# event | Low | Fires `OnPlayerDeath` |
| PowerUpSystem | C# event | Low | Subscribes to `OnPowerUpChosen` |
| AudioManager / JuiceManager | C# event | Low | Fires `OnPlayerHit`/`OnPlayerDeath` for them to consume |

---

## Projectile
**SRP**: Projectile is the bullet — both player shots and enemy fire.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| PlayerShip | Direct method call | High | Calls `TakeHit()` on collision (enemy bullet); PlayerShip spawns it |
| EnemyFireController | Direct method call | High | EnemyFireController acquires and initializes it from pool |
| Enemy | Direct method call | High | Calls `TakeDamage(damage)` on collision (player bullet) |
| Object Pool | Direct method call | High | Acquire on spawn, release on despawn |

---

## EnemyFormation
**SRP**: EnemyFormation owns the marching invader grid: it spawns the wave's Enemy children in a grid, moves the whole formation horizontally in lockstep, drops it down and reverses at the screen edges (classic Space Invaders march), and tracks how many invaders remain.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted`; fires `OnFormationCleared` |
| Enemy | Direct method call + C# event | High | Spawns/owns/moves Enemy children (High); subscribes to `OnEnemyKilled` (Low) |
| EnemyFireController | Property read | Low | Exposes live shooter candidate set |
| GameManager | C# event | Low | Formation-reaches-player-line triggers run-fail via agreed mechanism |

---

## Enemy
**SRP**: Enemy is a single invader: it holds hit points (tiered so front rows are tankier), takes damage from player projectiles, and on death awards points and fires `OnEnemyKilled`.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Projectile | Direct method call | High | Receives `TakeDamage(amount)` from player bullets |
| EnemyFormation | Direct method call + C# event | High | Spawned/owned/moved by EnemyFormation (High); fires `OnEnemyKilled` for it (Low) |
| ScoreSystem | C# event | Low | Fires `OnEnemyKilled` |
| AudioManager / JuiceManager | C# event | Low | Fires `OnEnemyKilled` |
| EnemyFireController | Property read | Low | Exposes position/muzzle for shooter selection |

---

## EnemyFireController
**SRP**: EnemyFireController is the brain behind enemy fire: it picks which living invaders shoot, decides the firing pattern and cadence, and spawns enemy Projectiles — scaling shooter count, fire rate, and pattern complexity per level so density ramps from sparse aimed shots (L1) to dense bullet-hell (L6).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted` for fire config |
| EnemyFormation | Property read | Low | Reads live shooter candidate set + muzzle positions |
| Projectile | Direct method call | High | Spawns enemy bullets from pool |
| PlayerShip | Property read | Low | Reads player X position for aimed patterns |

---

## LevelManager
**SRP**: LevelManager drives the run's 6 fixed levels (D5).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | C# event | Low | Subscribes to `OnLevelChanged`; fires `OnLevelCleared` |
| EnemyFormation | C# event | Low | Fires `OnLevelStarted`; subscribes to `OnFormationCleared` |
| EnemyFireController | C# event | Low | Fires `OnLevelStarted` |
| PowerUpSystem | C# event | Low | Fires `OnLevelCleared`; waits on `OnPowerUpChosen` |
| UIManager / AudioManager / JuiceManager | C# event | Low | Fires `OnLevelStarted`/`OnLevelCleared` |

---

## ScoreSystem
**SRP**: ScoreSystem tracks the run's score.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` |
| UIManager | C# event | Low | Fires `OnScoreChanged` |
| GameManager | C# event | Low | Subscribes to `OnRunStarted` to reset score |

---

## PowerUpSystem
**SRP**: PowerUpSystem is the run's roguelike layer.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| LevelManager | C# event | Low | Subscribes to `OnLevelCleared` |
| UIManager | C# event | Low | Fires `OnPowerUpOffered`; receives selection callback |
| PlayerShip | C# event | Low | Fires `OnPowerUpChosen` |
| GameManager | C# event + Direct method call | High | Subscribes to `OnRunStarted` (Low); `extra_life` adds a life via direct call (High) |

---

## UIManager
**SRP**: UIManager is the presentation layer: it renders the HUD (score, lives, level), the between-level power-up pick screen, and the game-over / victory / restart UI — all in UI Toolkit (.uxml + .uss, per best-practices.md).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| GameManager | C# event | Low | Subscribes to `OnRunStarted`/`OnRunEnded`/`OnLivesChanged`/`OnLevelChanged`; fires `OnRestartRequested` |
| ScoreSystem | C# event | Low | Subscribes to `OnScoreChanged` |
| PowerUpSystem | C# event | Low | Subscribes to `OnPowerUpOffered`; relays selection |
| SceneLoader | C# event | Low | Subscribes to `OnSceneLoaded` |

---

## AudioManager
**SRP**: AudioManager plays all music and sound effects in response to game events.

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` |
| PlayerShip | C# event | Low | Subscribes to `OnPlayerHit`/`OnPlayerDeath` |
| LevelManager | C# event | Low | Subscribes to `OnLevelStarted`/`OnLevelCleared` |
| GameManager | C# event | Low | Subscribes to `OnRunStarted`/`OnRunEnded` |
| ScoreSystem / PowerUpSystem | C# event | Low | Subscribes to score/combo/power-up events (optional) |

---

## JuiceManager
**SRP**: JuiceManager owns non-audio game feel: screen shake, particle bursts, and hit-stop (brief timeScale freezes).

| Coupled To | Type | Strength | Reason |
|---|---|---|---|
| Enemy | C# event | Low | Subscribes to `OnEnemyKilled` |
| PlayerShip | C# event | Low | Subscribes to `OnPlayerHit`/`OnPlayerDeath` |
| LevelManager | C# event | Low | Subscribes to `OnLevelCleared` |
| Camera | Inspector reference | Medium | Direct ref to apply/restore shake offset |
| Particle pools | Direct method call | High | Acquire/release pooled burst systems |
